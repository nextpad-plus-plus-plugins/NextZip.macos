# build output
build/
build-*/

# 7-Zip source + engine: fetched & built by deps/build-7z-engine.sh, not vendored
deps/7zip/
deps/7z.so
*.o
*.so
*.dylib

# internal design notes / investigation (local-only)
/docs/

# local scratch (icon experiments etc.)
/tmp/

.DS_Store
